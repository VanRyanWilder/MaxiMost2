name = "maximost-backend"
main = "src/worker.ts"
compatibility_date = "2024-06-11"

# This line enables Node.js compatibility, which is essential
compatibility_flags = [ "nodejs_compat" ]

# This section tells wrangler how to build the project
[build]
command = "npm run build"