# Environment variables for the Maximost Client (Vite)
# Copy this file to .env.local and fill in your Firebase project details.
# These variables are loaded by Vite and accessible in client-side code via import.meta.env.

# Firebase Configuration (from client/src/lib/firebase.ts)
VITE_FIREBASE_API_KEY="YOUR_FIREBASE_API_KEY"
VITE_FIREBASE_PROJECT_ID="YOUR_FIREBASE_PROJECT_ID"
VITE_FIREBASE_APP_ID="YOUR_FIREBASE_APP_ID"

# Example of another potential client-side variable (if any were used)
# VITE_API_BASE_URL="/api" # Though apiClient.ts currently hardcodes /api prefix for relative paths
